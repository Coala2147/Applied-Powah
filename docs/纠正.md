- 从 ME 网络取 FE：从同一缓冲抽 AE，再按 1 AE = 2 FE 折回 FE，以 Forge 能源能力（IEnergyStorage/IEnergyProvider）对外供给，给背包里的 FE 物品充电。
此点理解有误，ME网络抽取FE指的是来自applied flux的fe，这点必须纠正；
另外，丢失了：4. 我想要的是，比如最高级的powah充能棒通过合成，可以变为ME充能棒，这个ME充能棒可以直接抽取网络中的FE能量，或者围着一圈能源组件合成为AE充能棒，直接抽取AE网络中的AE能源（就是那个什么致密组件），这样子也避免了不装Applied Flux无法使用后的情况。但是这里我给出一个想法：原版的充能棒是一级一级升的，我们AE充能棒可不可以添加一系列配方直接从原版改为AE，随后AE充能棒同样也能一级一级升？我觉得这个方向我可能更喜欢一些；
也就是说，要有AE/ME充能棒的合成配方，但是她到底是抽取AE还是FE，我建议是添加两套机制，AE充能棒抽AE，ME充能棒抽FE，同时效仿原版保留一系列的升级配方，并且AE棒和FE棒可以相互转变，就直接丢存储台无需合成吧。至于贴图暂时使用powah的贴图。如果没有装applied flux，则只有AE棒
2.2没问题，暂时贴图借用AE2的致密（超密）、创造（极密）；如果没有装powah，mod不会报错，但是整个模组就只剩这几个能源组件了。
AE/ME棒视为类似能源组件和`AppliedGenerators`那个FE组件，一个可以被充能AE，一个可以直接从网络抽取FE，每次抽取的量可能需要抉择（这直接关乎充能台的充能速率）
便携充能棒的存在可圈可点，我初期想法是，在AE/FE充能棒的基础上进行合成，可以效仿量子环或无线终端的合成配方，就大概是加个无线接收器 (Wireless Receiver)，再辅以什么东西。
`4. 与"点 #2 风险（高吞吐误耗 → 关机）"的关系`主要是两方面：1. 直接抽取AE能源会导致controller等整个网络的能量耗尽，网络停机；2. 抽取网络内的FE吞吐量过大耗电，导致网络停机
